diff --git a/bae-bridge/src/handle.rs b/bae-bridge/src/handle.rs
--- a/bae-bridge/src/handle.rs
+++ b/bae-bridge/src/handle.rs
@@ -1164,6 +1164,10 @@ impl AppHandle {
                 cover,
                 bridge_storage_mode_to_core(storage_mode),
                 torrent_options,
+                // A2 will replace this with the user's identity choice.
diff --git a/bae-bridge/src/types.rs b/bae-bridge/src/types.rs
--- a/bae-bridge/src/types.rs
+++ b/bae-bridge/src/types.rs
@@ -515,6 +542,9 @@ pub enum BridgeIdentifyState {
         library_status: BridgeLibraryStatus,
         track_count: u32,
         source: BridgeIdentifySource,
+        /// User's identity claim derived from the matched search result.
+        /// A2 will replace this with the user's exact/approximate choice.
diff --git a/bae-core/src/db/client.rs b/bae-core/src/db/client.rs
--- a/bae-core/src/db/client.rs
+++ b/bae-core/src/db/client.rs
@@ -1024,10 +1024,10 @@ impl Database {
     /// Replaces album_artists, track_artists, and release_metadata rows
     /// entirely.
     ///
-    /// Identity rows on `release_identities` are not touched here —
-    /// rewriting identity is a separate operation. `metadata_source` /
-    /// `metadata_source_release_id` are similarly unchanged by this
-    /// method; they're updated alongside identity changes.
+    /// Identity rows on `release_identities` are not touched here — A5
diff --git a/bae-core/src/identify/discid.rs b/bae-core/src/identify/discid.rs
--- a/bae-core/src/identify/discid.rs
+++ b/bae-core/src/identify/discid.rs
@@ -176,11 +176,31 @@ pub enum TorrentPrologueProgress {
     },
 }
 
+/// Derive the per-source identity rows for a matched search result.
+/// Today: one row carrying the search result's `(source, source_group_id,
+/// release_id)`. Empty when the search result didn't surface a group.
+/// A2 will replace the source-of-values when the user picks
diff --git a/bae-core/src/identify/state.rs b/bae-core/src/identify/state.rs
--- a/bae-core/src/identify/state.rs
+++ b/bae-core/src/identify/state.rs
@@ -59,6 +60,10 @@ pub enum IdentifyState {
         library_status: LibraryStatus,
         track_count: u32,
         source: IdentifySource,
+        /// User's identity claim for this release. Today derived from the
+        /// matched search result (one row, source + group + release).
+        /// A2 will substitute the user's exact/approximate choice here.
diff --git a/bae-core/src/import/discogs_mapper.rs b/bae-core/src/import/discogs_mapper.rs
--- a/bae-core/src/import/discogs_mapper.rs
+++ b/bae-core/src/import/discogs_mapper.rs
@@ -125,14 +127,34 @@ pub fn map_discogs_to_db(
         tracks.push(track);
     }
 
-    Ok((
+    // Identity row for Discogs. The reverse cross-link (Discogs → MB) is
+    // plan X1: Discogs API doesn't expose MBIDs, so the MB row needs a
diff --git a/bae-core/src/import/handle.rs b/bae-core/src/import/handle.rs
--- a/bae-core/src/import/handle.rs
+++ b/bae-core/src/import/handle.rs
@@ -367,6 +443,11 @@ impl ImportServiceHandle {
     /// Remote cover bytes are not threaded through the command;
     /// `download_cover_art_bytes` consults the URL cache when the
     /// worker writes the cover.
+    ///
+    /// `identities` is the user's per-source identity claim. Empty
+    /// today — the worker initializes from the mapper output at commit
+    /// time (the single substitution point A2 will replace with the
diff --git a/bae-core/src/import/service.rs b/bae-core/src/import/service.rs
--- a/bae-core/src/import/service.rs
+++ b/bae-core/src/import/service.rs
@@ -689,21 +696,43 @@ impl ImportService {
     /// `prepare_release` — this function does pure DB work and string
     /// remapping, no network.
     ///
+    /// `command_identities` is the per-source identity vec carried on
+    /// the `ImportCommand`. Empty means "use the mapper's output" — the
+    /// substitution point A2 will use to inject the user's
diff --git a/bae-core/src/library/manager.rs b/bae-core/src/library/manager.rs
--- a/bae-core/src/library/manager.rs
+++ b/bae-core/src/library/manager.rs
@@ -1486,7 +1539,18 @@ impl LibraryManager {
         use crate::db::{DbReleaseMetadata, DbTrackArtist};
         use std::collections::HashMap;
 
-        let (new_album, new_release, new_tracks, artists, album_artists, track_artists) = metadata;
+        let crate::import::ParsedAlbum {
+            album: new_album,
+            release: new_release,
+            tracks: new_tracks,
+            artists,
+            album_artists,
+            track_artists,
+            // Identity replacement on re-fetch lands with the
+            // `set_identity` operation in plan A5; this metadata-only
