name: "PR #634 v1 — first reviewed commit (transient-ref batch)"
project: bae
diff_file: ../diffs/pr634-v1.diff

# bae-fm/bae#634, commit f835058f7 — first reviewed snapshot of the
# in-memory ReleaseIdentity work. At 2026-05-17 19:25-19:26 UTC, the
# bot caught 9 transient-ref findings on this commit, one per file.
#
# The original commit has since been amend-rebased away upstream — the
# diff below is assembled from the bot's inline-comment diff_hunks
# (only the changed regions around each finding, not the full PR
# diff). Enough context for the bot to identify each violation.
#
# `replay` pins the agent-instructions commit AND the model that
# actually ran. Production at that moment:
#   - agent-instructions HEAD: e8c37ee
#     (pre opus-switch, pre 4-part body format, pre swift carve-out
#     removal — the workflow's schema root key was `violations` not
#     `findings`)
#   - model: claude-sonnet-4-6 (default of anthropics/claude-code-action@v1
#     at that commit; no --model arg in claude_args)
replay:
  agent_instructions_sha: e8c37eee9e2e6984ea49ba021c847d35c50a7f29
  model: claude-sonnet-4-6

expected:
  - rule: "No transient references"
    path: bae-bridge/src/handle.rs
  - rule: "No transient references"
    path: bae-bridge/src/types.rs
  - rule: "No transient references"
    path: bae-core/src/db/client.rs
  - rule: "No transient references"
    path: bae-core/src/identify/discid.rs
  - rule: "No transient references"
    path: bae-core/src/identify/state.rs
  - rule: "No transient references"
    path: bae-core/src/import/discogs_mapper.rs
  - rule: "No transient references"
    path: bae-core/src/import/handle.rs
  - rule: "No transient references"
    path: bae-core/src/import/service.rs
  - rule: "No transient references"
    path: bae-core/src/library/manager.rs
